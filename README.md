# GitHub stars scripts

A collection of scripts to manage GitHub stars.

# Requirements

The Python scripts can be run in a venv after installing `requirements.txt`. The bash scripts require `jq` to be installed. 
